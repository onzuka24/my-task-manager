# Doc Standards Review — prd.md

**Target:** `_bmad-output/planning-artifacts/prds/prd-my-task-manager-2026-09-14/prd.md` (401 行 / 12,444 words)
**Lenses:** `structure` → `prose` (prose ran on top of the structural findings)
**Style baseline:** Microsoft Writing Style Guide + the document's own conventions (半角スペース around Latin, 全角句読点, ` — ` for em-dash asides)
**Reader calibration:** humans (作者本人) + downstream workflows (architecture / UX / epic breakdown)
**Scope:** editorial only. Product decisions, scope, and requirement content are treated as sacrosanct — no finding below asks for a requirement to change, be added, or be removed.

**Purpose read:** この文書は、作者本人と下流ワークフロー (アーキテクチャ設計・UX 設計・エピック分解) が、常駐型タスクマネージャの v1 を着手できるようにするために存在する。
**Structure model applied:** Strategic/Context (Pyramid) — PRD の標準形。Top-down、MECE、根拠は主張の後ろ。

**Counts:** 38 findings — High 7 / Medium 17 / Low 11 / Preserve 3 (= 35 actionable + 3 explicit PRESERVE)。
**Estimated reduction if all CUT/CONDENSE accepted:** 約 270 words (2.2%)。長さ目標の指定はなし。

---

## Pass 1 — Structure

### S-01 [Medium] MOVE — §6 の H2 が本文ゼロ、章全体を枠づける段落が §6.1 に埋まっている

**Location:** L311-313 (`## 6. MVP スコープ`) および L319-320
**word impact:** ±0 (移動のみ)

§6 は H2 直下の本文が 0 words。一方 L320 の段落は §6.1・§6.2・§6.3 の三つすべてを支配する前提 (「v1 が検証する仮説は一つ」) であり、§6.1 の中に閉じ込められている。Pyramid モデルの「結論が先」に反し、空見出しも生む。

**Current (L311-313):**
```
## 6. MVP スコープ

### 6.1 スコープ内 (v1)
```

**Replacement:**
```
## 6. MVP スコープ

v1 が検証する仮説は一つである — **中断地点を記録し再開時に全体の中の位置を返すことで、前のタスクを実際に頭から手放せるようになるか。** この仮説が成立しなければ、**本日計画**も**観測**も**交差**も意味を持たない。したがって最も安価にこの一点を検証できる構成を v1 とする。

### 6.1 スコープ内 (v1)
```

**併せて L319-320 (空行 + 当該段落) を §6.1 から削除する。**

---

### S-02 [High] CUT — OQ-1 の「撤回」の経緯は `addendum.md` §3.1 に既出

**Location:** L387
**word impact:** −45

§0 は「検討経緯とともに `addendum.md` に分離した」と宣言している。しかし OQ-1 は判断の撤回という経緯そのものを本文に持つ。同じ撤回は `addendum.md` L46 に完全な形で存在する (「当初「v1 に FR-11 が含まれないためプラットフォーム決定は v1 の着手を阻害しない」と記載していたが、これは誤りであり撤回する。」)。PRD 側は現在の結論だけを述べればよい。

**Current:**
```
- **OQ-1 [v1]: 対象プラットフォームが未確定である。** 当初は「**観測**が v2 であるため v1 の着手は阻害されない」と判断していたが、これは誤りであり撤回する。FR-1 (グローバルホットキーの捕捉)、FR-3 (ログイン時の自動起動)、FR-15 (最前面での強い提示) はいずれも v1 スコープかつ OS 固有の統合を要する (§8 可搬性)。**v1 の実装着手前に決定を要する。**
```

**Replacement:**
```
- **OQ-1 [v1]: 対象プラットフォームが未確定である。** FR-1 (グローバルホットキーの捕捉)、FR-3 (ログイン時の自動起動)、FR-15 (最前面での強い提示) はいずれも v1 スコープかつ OS 固有の統合を要する (§8 可搬性)。**v1 の実装着手前に決定を要する。** 判断の経緯は `addendum.md` §3.1 に記録した。
```

---

### S-03 [High] CONDENSE — 本文中の `[NOTE FOR PM]` / `[ASSUMPTION]` が §10 の OQ を丸ごと重複している

**Location:** L227 (= OQ-6)、L278 (= OQ-5)、L344 (= OQ-4)
**word impact:** −70

§10 は「下流から参照されるため ID を与える」と宣言し、未決事項の単一の置き場になっている。しかし上記 3 箇所は OQ の本文をそのまま二重に書いている。二重記述は必ずどちらかが先に古くなる。本文側はポインタに落とす。

**S-03a — Current (L227):**
```
- **固着**の検出は**介入**の契機となる。[NOTE FOR PM] **固着**に対する**介入**の提示内容は v2 の設計課題として未定である。
```
**Replacement:**
```
- **固着**の検出は**介入**の契機となる。ただし**介入**の提示内容は未定である (§10 OQ-6)。
```

**S-03b — Current (L278):**
```
**注記:** [NOTE FOR PM] 記録された決定の活用方法 (振り返り画面、傾向分析等) は v1 では定義していない。記録のみ行う。
```
**Replacement:**
```
**注記:** 記録された決定の活用方法は v1 では定義せず、記録のみを行う (§10 OQ-5)。
```
(より強い選択肢: L277-278 を空行ごと削除する。同じ内容は §6.2 L329 と §10 OQ-5 L391 の 2 箇所に既にある。なお §4 の 21 個の FR のうち `**注記:**` ブロックを持つのは FR-16 だけであり、テンプレートの例外でもある。)

**S-03c — Current (L344):**
```
- **観測**により取得したウィンドウ識別情報には保持期限を設け、無期限に蓄積しない。[ASSUMPTION: 具体的な保持期間は未定 — 要確認]
```
**Replacement:**
```
- **観測**により取得したウィンドウ識別情報には保持期限を設け、無期限に蓄積しない。具体的な保持期間は未定である (§10 OQ-4)。
```

---

### S-04 [Medium] CUT — §11 の `§7.1` 項目は前提ではなく未解決の問いであり、OQ-4 と重複する

**Location:** L400
**word impact:** −15

§11 は「本文中の `[ASSUMPTION]` タグを集約したもの」= 未確認の「推論」の一覧。「保持期間が未定である」は推論ではなく未決事項であり、OQ-4 (L390) が既に保持している。S-03c を適用すれば本文側のタグも消えるため、この項目の根拠自体がなくなる。

**Current:**
```
- **§7.1** — **観測**データの保持期間は未定である (OQ-4)。
```
**Replacement:** 行ごと削除。

---

### S-05 [Medium] CONDENSE — §11 が本文の `[ASSUMPTION]` の理由文をほぼ逐語で再掲している

**Location:** L399 (本文側は L199)
**word impact:** −30

§0 は §11 を「集約」と説明しているが、現状は要約ではなく複製である (L199 と L399 は理由まで同一)。§11 を「該当箇所 + 一行の前提 + ポインタ」に統一すれば、理由の単一の置き場は本文側に残る。

**Current (L399):**
```
- **§4.4 FR-10** — **本日計画**の未作成に対して督促の通知を行わない。督促自体が反射的に無視される**介入**となり、**介入**全体の信頼性を損なうと判断した。
```
**Replacement:**
```
- **§4.4 FR-10** — **本日計画**の未作成に対して督促の通知を行わない (理由は FR-10 の `[ASSUMPTION]` に記載)。
```

---

### S-06 [Medium] MOVE — §4.5 で中核である FR-13 (交差の検出) が末尾に埋まっている

**Location:** L205-243 (§4.5 内の FR 配列)

現在の並びは FR-11 → FR-12 → FR-20 → FR-21 → FR-13。§3 は**交差**を「本ツールが検出対象とする中心的な状態」と定義し、§4.6 の FR-14、UJ-2、OQ 群がすべてこれを前提にする。その中心が周辺的な 2 検出の後ろに置かれており、front-load 原則に反する。また FR-12 (ずれ) → FR-13 (ずれの特殊形) は定義上連続しており、間に FR-20/FR-21 が挟まるのは依存順にも反する。

**Replacement:** L237-243 の `#### FR-13: 交差の検出` ブロック全体を、L219 の直後 (FR-12 の末尾) へ移動し、§4.5 の並びを **FR-11 → FR-12 → FR-13 → FR-20 → FR-21** にする。FR 番号は §4 の方針どおり振り直さない。§6.2 L327 の列挙も併せて `(FR-11、FR-12、FR-13、FR-20、FR-21)` の順に揃える。

**Current (L327):**
```
- **4.5 観測とずれ検出** (FR-11、FR-12、FR-13、FR-20、FR-21) — v2 以降。実装コストとプラットフォーム依存が最大であり、かつ中核仮説の検証に不要。
```
**Note:** L327 は既にこの順序。移動によって本文と一致する。

---

### S-07 [Medium] 見出し/導入が内容を取りこぼしている — §4.5 の「説明」が配下 5 FR のうち 2 つしか述べていない

**Location:** L203

見出しは「観測とずれ検出」だが、説明文は**観測**と**固着**の推定しか述べず、**ずれ** (FR-12) と**交差** (FR-13) — 見出しが名指ししている当のもの — に触れていない。

**Current:**
```
**説明:** **常駐プロセス**は最前面のアプリケーション/ウィンドウを観測し、ユーザーが宣言しなかった**切り替え**および固着状態を推定する。これは本ツールが他のタスク管理ツールと決定的に異なる点であり、同時に最大の実装コストとプライバシー上の懸案を伴う。
```

**Replacement:**
```
**説明:** **常駐プロセス**は最前面のアプリケーション/ウィンドウを**観測**し、その結果を**現在地**および**本日計画**と突き合わせて、**ずれ**・**交差**・**固着**・未宣言の**切り替え**の四つを検出する。これは本ツールが他のタスク管理ツールと決定的に異なる点であり、同時に最大の実装コストとプライバシー上の懸案を伴う。
```

---

### S-08 [Medium] 見出しが内容を誤記述 — §4.1「常駐と呼び出し」に FR-2 (既定表示の最小化) が入っている

**Location:** L89 (見出し)、L91 (説明)

FR-2 は表示方針 (設計上の賭け #1) であって、常駐でも呼び出しでもない。§4.1 の説明文も呼び出しコストの話に終始し、FR-2 を全く扱っていない — 配下 3 FR のうち 1 つが導入文の射程外にある。FR を動かさずに見出しと説明を実態に合わせるのが最小の修正。

**Current (L89):**
```
### 4.1 常駐と呼び出し
```
**Replacement:**
```
### 4.1 常駐・呼び出し・既定表示
```

**Current (L91, 末尾):**
```
ここでの設計原則は「ツールを開くことが中断であってはならない」であり、したがって呼び出しから記録完了までの操作は数秒で完結しなければならない。
```
**Replacement:**
```
ここでの設計原則は「ツールを開くことが中断であってはならない」であり、したがって呼び出しから記録完了までの操作は数秒で完結しなければならない。開いた**オーバーレイ**が既定で何を見せるかも本節が定める。
```

**併せて §6.1 L315 の見出し名を更新する。**
**Current (L315):** `- **4.1 常駐と呼び出し** — FR-1、FR-2、FR-3`
**Replacement:** `- **4.1 常駐・呼び出し・既定表示** — FR-1、FR-2、FR-3`

---

### S-09 [Medium] MERGE — §2.2 は恒久的な非目標であり、見出しの「v1 で」が内容を誤記述している。§5 と重複する

**Location:** L48-51 (§2.2)、L307 (§5)
**word impact:** −25

§2.2 の 2 項目はいずれも「v1 で」の限定ではなく恒久的な除外であり (§5 が同じことを非目標として宣言している)、見出しがバージョン限定を約束してしまっている。§5 L307 は既に §2.2 を参照しており、参照先が 25 words しかないため一段の往復が無駄になっている。「やらないこと」の置き場が §2.2 / §5 / §6.2 の三つに割れている状態も解消できる (§6.2 はバージョン延期であり別物として残す)。

**Current (L48-51):**
```
### 2.2 v1 で対象としないもの

- チームでの利用、タスクの共有・割り当て・進捗報告。
- 他者に見せるための記録 (工数報告、実績管理)。
```
**Replacement:** §2.2 を節ごと削除し、`### 2.3 主要ユーザージャーニー` を `### 2.2 主要ユーザージャーニー` に繰り上げる (§2.3 への参照は文書内に存在しないため安全)。

**Current (L307):**
```
- **他者に見せるための記録を作らない。** 工数報告、実績管理、チーム共有は対象外である (§2.2)。
```
**Replacement:**
```
- **他者に見せるための記録を作らない。** 工数報告、実績管理、およびチームでの共有・割り当て・進捗報告はいずれも対象外である。
```

---

### S-10 [Medium] MOVE — FR-19 の「結果 (検証可能)」に検証不能な依存関係の記述が混入

**Location:** L156

「FR-9、FR-17、FR-18 はいずれも本開示面を前提とする」は依存関係の宣言であり、検証できる結果ではない。21 個の FR のうち、検証不能な項目が `結果 (検証可能)` の直下に置かれているのはここだけ。

**Current (L152-156):**
```
**結果 (検証可能):**
- 開示面は**オーバーレイ**の初期表示から到達するために、ユーザーの明示的な操作を最低 1 回必要とする。
- 開示面は**オーバーレイ**を閉じた時点で破棄され、次回の呼び出し時に初期表示 (FR-2) へ戻る。開示状態は保持されない。
- 開示面は**腐敗**した**タスク**を含まない (FR-18 の経路と区別される)。
- FR-9、FR-17、FR-18 はいずれも本開示面を前提とする。
```
**Replacement:** 最終行を削除し、本文段落 (L150) の末尾に移す。

**Current (L150):**
```
ユーザーは明示的な操作により、**タスク**および**ステップ**の一覧を開示できる。この面は既定では表示されず、FR-2 の最小表示から意図的な操作を経てのみ到達する。
```
**Replacement:**
```
ユーザーは明示的な操作により、**タスク**および**ステップ**の一覧を開示できる。この面は既定では表示されず、FR-2 の最小表示から意図的な操作を経てのみ到達する。FR-9、FR-17、FR-18 はいずれも本開示面を前提とする。
```

---

### S-11 [Medium] 見出し階層の不統一 — §9 だけが太字の擬似見出しでグループ分けしている

**Location:** L368、L373、L377

§1・§2・§6・§7 は `###` の番号付き小見出しを使う。751 words ある §9 だけが `**主要指標**` 形式の太字行で 3 グループに分かれており、目次にも出ず、下流からの参照点にもならない。

**Current:** `**主要指標**` / `**副次指標**` / `**逆指標 (最適化してはならない)**`
**Replacement:** `### 9.1 主要指標` / `### 9.2 副次指標` / `### 9.3 逆指標 (最適化してはならない)`

`**測定の前提** — …` (L366) はグループ見出しではなく §9 全体への前置きなので、太字リード文のまま 9.1 の前に残す。

---

### S-12 [Low] 番号付けの例外 — §1 の H3 だけ番号がない

**Location:** L29
**Current:** `### 設計上の三つの賭け`
**Replacement:** `### 1.1 設計上の三つの賭け`

文書内の他の H3 (2.1、2.3、6.1〜6.3、7.1〜7.3) はすべて番号付き。この賭けは §4 から「設計上の賭け #1 / #2」として 3 回参照されるため、番号付きの参照点を持つ価値がある。

---

### S-13 [Low] CONDENSE — §5 の 2 つ目の項目が §1 の賭け #1 の根拠を再掲している

**Location:** L305
**word impact:** −15

**Current:**
```
- **常時の一覧表示を持たない。** 「今日やること全部」「今週の予定」が黙って目に入る画面は設計目標に含まれない。一望できることが集中を奪ったという失敗経験が出発点である。明示的な操作による開示面 (FR-19) はこの非目標と両立する — 禁じているのは一覧の存在ではなく、求めていないときに一覧が視界に入ることである。
```
**Replacement:**
```
- **常時の一覧表示を持たない。** 「今日やること全部」「今週の予定」が黙って目に入る画面は設計目標に含まれない (設計上の賭け #1)。明示的な操作による開示面 (FR-19) はこの非目標と両立する — 禁じているのは一覧の存在ではなく、求めていないときに一覧が視界に入ることである。
```

---

### S-14 [Low] CONDENSE — FR-6 の 2 つ目の結果が §3 の定義をそのまま繰り返している

**Location:** L146
**word impact:** −20

**Current:**
```
- **現在地**が値を持たない状態は未着手時に限られる。**休息**中は値を保持したまま**非活性**として表現される (§3 参照)。
```
**Replacement:**
```
- **現在地**が値を持たない状態は未着手時に限られ、**休息**中も値は保持される。
```

---

### S-15 [Low] QUESTION — FR-18 が兄弟の FR-17 に比べて薄い

**Location:** L293-298 (FR-18: 93 words / 結果 1 項目) vs L284-291 (FR-17: 148 words / 結果 3 項目)

FR-18 の唯一の結果 (「明示的な操作を必要とする」) は要求文をほぼ言い換えただけで、復帰後の**現在地**や**腐敗**判定のリセットといった観測可能な帰結がない。§4 の他の 20 FR は 2〜4 項目を持つ。**内容の追加は編集の範囲外のため置換文は提案しない** — 深さの不均衡として作者の判断に委ねる。

---

### S-16 [PRESERVE] §3 用語集 (1,180 words / 全体の 9.5%) を §4 の前に置く構成

最大の単一セクションであり、Pyramid モデルでは通常このサイズの参照材は後方か付録に回る。しかし (a) §0 が「§3 が語彙を固定し、以降の全セクションはそこで定義された語のみを使用する」と明示的に契約しており、(b) 下流のアーキテクチャ/UX/エピック分解がこの語彙に依存する。**現状維持を推奨する。** 短縮も移動も提案しない。

---

### S-17 [PRESERVE] §0 L17「先行する入力文書は存在しない」

一見メタ情報だが、下流ワークフローに「ブリーフを探しても無い」と伝える実用情報。20 words。**残す。**

---

### S-18 [PRESERVE] §11 前提一覧そのもの

S-05 で個々の項目は圧縮を提案したが、一覧自体は「誤っていれば再検討を要する箇所」のチェックリストとして機能しており、reinforcement であって redundancy ではない。**節ごと残す。**

---

## Pass 2 — Prose

構造パスで CUT を提案した箇所 (L278、L400、§2.2、L156 の最終行) には校正を当てていない。MERGE 先 (§5 L307) の修正は S-09 の置換文に織り込み済み。

### P-01 [High] 同一概念に 2 つの規範的な表記が並立している — 位置情報のフォーマット

**Location:** L109 (FR-2) と L177 (FR-8)

どちらも `結果 (検証可能)` の中、すなわち UX 実装がそのまま文字列として拾う規範記述でありながら、形式が食い違う。下流でラベルが 2 種類生まれる。

**Current (L109):**
```
- ただし**現在地**の所在を示す位置情報 (例: 「No.3 / 全 6」) は初期表示に含まれる。
```
**Current (L177):**
```
- 提示内容に「第 N ステップ / 全 M ステップ」の形式の位置情報が含まれる。
```
**Replacement (L109):**
```
- ただし**現在地**の所在を示す位置情報 (例: 「第 3 ステップ / 全 6 ステップ」) は初期表示に含まれる。
```
(L177 を正とする案。逆に L109 を正とするなら L177 を 「「No.N / 全 M」の形式の位置情報」 に揃える。どちらでもよいが、**片方に統一すること**が要点。)

---

### P-02 [High] 壊れた組版 — `]` の直後にスペースがなく、`[ASSUMPTION]` が文を分断している

**Location:** L358 (§8 常時稼働)

文書内の他の 2 箇所 (L199、L344) では `[ASSUMPTION: … — 要確認]` が行末にある。ここだけ文中にあり、閉じ括弧の直後にスペースなしで本文が続く (`要確認]常駐を…`)。文書の「Latin 前後に半角スペース」規約にも反し、読点なしで 2 文が衝突している。

**Current:**
```
- **常時稼働** — **常駐プロセス**は終日動作する。待機時 (オーバーレイ非表示かつ介入なしの状態) の資源消費を CPU 1% 未満・メモリ 100MB 未満とする。[ASSUMPTION: この二つの数値および FR-1 の 300ms は実測に基づかない設定値である — 要確認]常駐を利用者に意識させないことが要件であり、この数値は検証可能な形でそれを表現したものである。当該上限は v1 の技術選定に対する制約として働く。
```
**Replacement:**
```
- **常時稼働** — **常駐プロセス**は終日動作する。待機時 (**オーバーレイ**非表示かつ**介入**なしの状態) の資源消費を CPU 1% 未満・メモリ 100MB 未満とする。常駐を利用者に意識させないことが要件であり、この数値は検証可能な形でそれを表現したものである。当該上限は v1 の技術選定に対する制約として働く。[ASSUMPTION: この二つの数値および FR-1 の 300ms は実測に基づかない設定値である — 要確認]
```
(タグを行末へ移動し、他 2 箇所と位置を揃えた。**オーバーレイ**/**介入**の太字化は P-08 と同一の修正。)

---

### P-03 [High] 未定義の用語 — 「作業文脈」が §3 に無いまま 3 箇所で使われている

**Location:** L80 (§3 固着の定義内)、L223 (FR-20)、L231 (FR-21)

§3 は「以降の全セクションは本節の用語を厳密に使用する。同義語の導入は禁止する」と宣言している。しかし**固着**の定義自体が、定義されていない「作業文脈」の上に乗っている。FR-20 の閾値判定は「作業文脈が変化しない」ことを条件にしており、下流はこれを実装可能な述語に落とさなければならない。

**Replacement:** §3 に 1 項目を追加する。L79 (**観測**) の直後、L80 (**固着**) の前に挿入:
```
- **作業文脈 (Work Context)** — **観測**により得られた、最前面のアプリケーション名とウィンドウタイトルの組。同一とみなす粒度は v1 の対象外である。
```
(粒度の判断は内容に属するため、必要なら `[ASSUMPTION]` か OQ として扱うこと。ここでは語の存在のみを編集上の欠落として指摘する。)

---

### P-04 [High] ID 形式の不統一 — UJ だけがコロンではなくピリオド区切り

**Location:** L57、L58、L59

FR は `FR-1:`、OQ は `OQ-1 [v1]:`、SM は `SM-1:` とコロン区切り。UJ だけが `UJ-1.` とピリオド。「FR は本 ID を参照する」と宣言している ID 体系の中で唯一の例外。

**Current:**
```
- **UJ-1. 割り込みから戻る。**
- **UJ-2. 交差に気づく。**
- **UJ-3. 休息に追い出される。**
```
**Replacement:**
```
- **UJ-1: 割り込みから戻る。**
- **UJ-2: 交差に気づく。**
- **UJ-3: 休息に追い出される。**
```

---

### P-05 [High] 凡例と実際の記法が一致していない — §10 の `[v1]` 印

**Location:** L385 (凡例) vs L387-393 (実際の項目)

凡例は「**v1** 印」(太字の v1) と書いているが、項目で使われているのは `[v1]` (角括弧付き)。読者は凡例の記法を本文中に探すことになる。あわせて、この行はイタリック中に太字を入れ子にした `*… **v1** 印は …*` の形になっており、文書内で唯一の入れ子強調でもある。

**Current:**
```
*下流から参照されるため ID を与える。**v1** 印は v1 の着手前に決定を要するもの、**v2** 印はそれ以降で足りるものを指す。*
```
**Replacement:**
```
*下流から参照されるため ID を与える。`[v1]` 印は v1 の着手前に決定を要するもの、`[v2]` 印はそれ以降で足りるものを指す。*
```

---

### P-06 [Medium] OQ 項目の太字終端が 2 系統に割れている

**Location:** L389、L390、L392 が一方の形、L387、L388、L391、L393 がもう一方

OQ-3 / OQ-4 / OQ-6 は `…未定である** (FR-17)。` と、太字を閉じてから参照と句点を置く。OQ-1 / OQ-2 / OQ-5 / OQ-7 は `…未定である。**` と句点を太字内に含める。

**Current (L389):**
```
- **OQ-3 [v2]: 腐敗の判定期間の初期値が未定である** (FR-17)。
```
**Replacement:**
```
- **OQ-3 [v2]: 腐敗の判定期間の初期値が未定である (FR-17)。**
```

**Current (L390):**
```
- **OQ-4 [v2]: 観測データの保持期間が未定である** (§7.1)。**観測**の取得対象の粒度は §3 で定義済みであるため、残る未決は期間のみである。
```
**Replacement:**
```
- **OQ-4 [v2]: 観測データの保持期間が未定である (§7.1)。** **観測**の取得対象の粒度は §3 で定義済みであるため、残る未決は期間のみである。
```

**Current (L392):**
```
- **OQ-6 [v2]: 固着に対する介入の提示内容が未定である** (FR-20)。**固着**の検出条件は定義したが、検出後にユーザーへ何を示すかは決まっていない。
```
**Replacement:**
```
- **OQ-6 [v2]: 固着に対する介入の提示内容が未定である (FR-20)。** **固着**の検出条件は定義したが、検出後にユーザーへ何を示すかは決まっていない。
```

---

### P-07 [Medium] §6 のリスト項目形式が §6.1 と §6.2 で揃っていない。FR ID の表記も見出しと違う

**Location:** L315-318、L326-330、L334

§6.1 は `**4.1 常駐と呼び出し** — FR-1、FR-2、FR-3` (FR をダッシュの後に列挙)、§6.2 は `**4.4 本日計画** (FR-9、FR-10) — v2。…` (FR を括弧内)。さらに `**FR-15 休息介入**` はコロンなしで、見出しの `#### FR-15: 休息介入` と食い違う。

**Replacement (L315-318):**
```
- **4.1 常駐・呼び出し・既定表示** (FR-1、FR-2、FR-3)
- **4.2 タスクとステップ** (FR-4、FR-5、FR-6、FR-19)
- **4.3 切り替えと再開** (FR-7、FR-8)
- **FR-15: 休息介入** — ただし §6.3 の縮退仕様による
```
(L315 の見出し名変更は S-08 と同一。L318 の「後述の縮退仕様」を明示参照 §6.3 に変えたのは P-11。)

**Replacement (L328、L329、L334 の先頭):** `**FR-14 交差介入**` → `**FR-14: 交差介入**`、`**FR-16 決定の記録**` → `**FR-16: 決定の記録**`、`**FR-15 休息介入**` → `**FR-15: 休息介入**`。L322 の `**FR-15 休息介入のみがこの論理の外にある。**` も `**FR-15: 休息介入のみがこの論理の外にある。**` とするか、あるいは文なので `**FR-15 (休息介入) のみがこの論理の外にある。**` とする。

---

### P-08 [Medium] §4 以降の本文で用語の太字が抜けている箇所がある

**Location:** L182、L199、L203、L243、L334、L358 (本文)、L68 (§3 内)

文書の規約は「§3 の用語は本文で太字」であり、90% 以上の箇所で守られている。以下が例外。なお §1 (用語集より前) と §2.3 の UJ 叙述は地の文として意図的に太字を外していると読めるため、**対象外とする**。

| 行 | Current (該当部分) | Replacement |
|---|---|---|
| L68 | `オーバーレイが何も操作されていない状態で` | `**オーバーレイ**が何も操作されていない状態で` |
| L182 | `本ツールの介入機能は**本日計画**に依存する` | `本ツールの**介入**機能は**本日計画**に依存する` |
| L199 | `それ自体が反射的に無視される介入となり介入全体の信頼性を下げるため` | `それ自体が反射的に無視される**介入**となり、**介入**全体の信頼性を下げるため` |
| L203 | `ユーザーが宣言しなかった**切り替え**および固着状態を推定する` | S-07 の置換文で解消 |
| L243 | `(完了を待てば交差は既に解消している)` | `(完了を待てば**交差**は既に解消している)` |
| L334 | `連続作業時間は「直近に宣言された**切り替え**からの経過時間」で代替する` | `**連続作業時間**は「直近に宣言された**切り替え**からの経過時間」で代替する` |
| L358 | `待機時 (オーバーレイ非表示かつ介入なしの状態)` | P-02 の置換文で解消 |

L199 の置換は読点を 1 つ補っている (「介入となり介入全体の」は同語が無読点で連続して読みにくい)。

---

### P-09 [Medium] 日本語文中に未訳の英単語が残っている

**Location:** L23 (`residue`)、L322 (`risk`)

`addendum.md` §1 は同じ概念を「**attention residue** (注意残余)」と正式に導入している。PRD 側は語をむき出しで使っており、初出の定義もない。`risk` は「リスク」で足りる。

**Current (L23, 該当部分):**
```
そして切り替えた後も前のタスクが頭に residue として残り続け、新しいタスクに注意を全量投入できない。
```
**Replacement:**
```
そして切り替えた後も前のタスクが頭に注意残余 (attention residue) として残り続け、新しいタスクに注意を全量投入できない。
```

**Current (L322, 該当部分):**
```
仮説検証には寄与せず、§6.3 の縮退により誤発火の risk を v1 に持ち込む。
```
**Replacement:**
```
仮説検証には寄与せず、§6.3 の縮退により誤発火のリスクを v1 に持ち込む。
```

---

### P-10 [Medium] `capture` / `grooming` は固有の設計原則名だが、初出で説明されていない

**Location:** L33 (初出)、L291 (再使用)

`addendum.md` §4 が「摩擦は capture ではなく grooming にある」を名前付きの原則として扱っているため、**語そのものは保存すべき** (訳出は提案しない)。ただし PRD 単体では L291「確認自体が grooming の再発であるため」が語の定義なしに登場する。初出で一度だけ括弧注を付ける。

**Current (L33):**
```
3. **摩擦は capture ではなく grooming にある。** 書き留めること自体は苦ではない。
```
**Replacement:**
```
3. **摩擦は capture (書き留め) ではなく grooming (リストの手入れ) にある。** 書き留めること自体は苦ではない。
```

---

### P-11 [Medium] 曖昧な前方参照 — 「後述の縮退仕様」

**Location:** L318
**Current:** `- **FR-15 休息介入** — ただし後述の縮退仕様による`
**Replacement:** `- **FR-15: 休息介入** — ただし §6.3 の縮退仕様による`

文書の他の箇所はすべて明示的な § 参照を使っている。(P-07 の置換文に統合済み。)

---

### P-12 [Medium] `〜` 前後のスペースが 3 箇所で不統一

**Location:** L32 (`No.1〜N`)、L87 (`FR-1 〜 FR-21`)、L366 (`2〜3 行`)

3 つのうち L87 だけが `〜` の両側に半角スペースを持つ。全角の波ダッシュは前後スペース不要が標準であり、多数派 (2/3) にも合う。

**Current (L87, 該当部分):** `FR は文書全体で通し番号を持つ (FR-1 〜 FR-21)。`
**Replacement:** `FR は文書全体で通し番号を持つ (FR-1〜FR-21)。`

---

### P-13 [Medium] §0 が `[NOTE FOR PM]` マーカーを説明していない

**Location:** L15

§0 は `[ASSUMPTION]` の意味と集約先 (§11) を説明するが、本文に 4 回現れる `[NOTE FOR PM]` (L227、L278、L329、L349) には一切触れない。S-03 を適用しても L329 と L349 は残る。

**Current (該当文):**
```
未確認の推論は本文中に `[ASSUMPTION]` として埋め込み、§11 に集約した。
```
**Replacement:**
```
未確認の推論は本文中に `[ASSUMPTION]` として埋め込み、§11 に集約した。下流への申し送りは `[NOTE FOR PM]` として本文中に置き、未決事項であるものは §10 に ID 付きで集約した。
```

---

### P-14 [Low] 同語反復 — 「§4 機能では機能ごとに」

**Location:** L15
**Current:** `§4 機能では機能ごとにまとまりを作り、その下に機能要件 (FR) を通し番号で入れ子にする。`
**Replacement:** `§4 機能では機能領域ごとにまとまりを作り、その下に機能要件 (FR) を通し番号で入れ子にする。`

---

### P-15 [Low] 指示対象が不明瞭 — 「FR は本 ID を参照する」

**Location:** L55
**Current:** `*solo 用途のため軽量表記とする。FR は本 ID を参照する。*`
**Replacement:** `*solo 用途のため軽量表記とする。§4 の各 FR は本節の UJ-n を参照する。*`

「本 ID」が UJ-n を指すことは文脈から推測できるが、明示されていない。

---

### P-16 [Low] 人称の不統一 — §2.3 だけが固有名と三人称代名詞を使う

**Location:** L57、L58、L59

文書全体は「ユーザー」または「作者自身」で通しているが、§2.3 だけ「Onzuka」(3 回) と「彼」(2 回) が現れ、§2 冒頭で導入もされていない。叙述体である UJ の意図的な文体と読めるため**強い変更は提案しない**が、統一するなら:

**Current (L57, 該当部分):** `作業中に別件が差し込まれ、Onzuka はホットキーでオーバーレイを呼び出し`
**Replacement:** `作業中に別件が差し込まれ、ユーザーはホットキーでオーバーレイを呼び出し`
(L58 の `Onzuka は` → `ユーザーは`、`彼は` → `ユーザーは`; L59 も同様。)

もしくは §2 冒頭 L37 で一度だけ名前を導入する: `本ツールの利用者は作者自身 (以下 Onzuka) ただ一人である (solo / personal)。`

---

### P-17 [Low] 同一事項に 3 つの呼び名 — 製品名の仮称

**Location:** L9、L393

**Current (L9):** `*ワーキングタイトル — 要確認*`
**Replacement:** `*仮称 — 未確定 (§10 OQ-7)*`

L393 が「作業用の仮称」と書いているため、カタカナ語の「ワーキングタイトル」を残す理由がない。OQ-7 への参照も付く。

---

### P-18 [Low] `§7.2` の 1 項目に 3 つの独立した主張が詰まっている

**Location:** L349

§7.2 は 1 bullet のみで 154 words。禁止事項 + `[NOTE FOR PM]` + v1 スコープ判断 + OQ 参照が一文の塊になっている。§7.1 が 4 bullets に分けているのと不均衡。

**Current:**
```
- **介入** (FR-14、FR-15) は意図的に強い提示形式を取るが、他アプリケーションの操作を奪って不可逆な結果を招いてはならない。特にフルスクリーンでの作業中および入力中に、入力先を奪ってはならない。[NOTE FOR PM] 強い提示と入力先の非奪取を両立させる手段は本 PRD では決定せず、UX 設計に委ねる。FR-15 が v1 スコープであるため、これは v1 の設計課題である (§10 OQ-2)。
```
**Replacement:**
```
- **介入** (FR-14、FR-15) は意図的に強い提示形式を取るが、他アプリケーションの操作を奪って不可逆な結果を招いてはならない。
- 特にフルスクリーンでの作業中および入力中に、入力先を奪ってはならない。
- [NOTE FOR PM] 強い提示と入力先の非奪取を両立させる手段は本 PRD では決定せず、UX 設計に委ねる。FR-15 が v1 スコープであるため、これは v1 の設計課題である (§10 OQ-2)。
```

---

### P-19 [Low] 「設計上の賭け」への参照記法が 2 通り

**Location:** L104 (`設計上の賭け #1 を実装する`)、L109 (`設計上の賭け #2 に基づく`)、L121 (`(設計上の賭け #2)`)

`#n` 記法自体は統一されているが、§1 の元リストは Markdown の番号付きリスト (1./2./3.) であり、`#1` という ID は明示的に定義されていない。S-12 で `### 1.1 設計上の三つの賭け` に番号を与えるなら、L31-33 の各項目に括弧 ID を付けると参照が閉じる。

**Current (L31, 冒頭):** `1. **一度に見せるのは「次の一手」だけ。**`
**Replacement:** `1. **(#1) 一度に見せるのは「次の一手」だけ。**`
(L32 は `(#2)`、L33 は `(#3)`。)

---

### P-20 [Low] 残りの軽微な指摘 — まとめ

- **L21** `**タスクとタスクの「あいだ」を扱うための常駐ツール**である。` — 太字が「である」の直前で切れており、文末が強調から外れる。`**タスクとタスクの「あいだ」を扱うための常駐ツールである。**` に揃えると §1 の他の太字文 (L23、L25) と一致する。
- **L66** `タスクに対して 1:N。` / **L69** `ステップに対して 0..1。` / **L70** `一日に対して 0..1。` — 多重度記法が `1:N` と `0..1` の 2 系統。`0..N` / `0..1` (UML 記法) に統一するのが簡潔。**L66 Replacement:** `タスクに対して 1..N。`
- **L66、L128** — §3 と FR-4 の中で `タスク` が太字化されていない (`タスク内で No.1 から…`)。定義項目内の自己言及なので許容範囲だが、P-08 と同じ規約の例外。
- **L190** — 1 文が 90 字を超え、括弧内に因果の説明が入る二重構造。読点位置は妥当なので**変更は提案しない**。
- **`addendum.md` との齟齬 (本ファイルの編集対象外、参考)** — addendum §3.3 は「未解決の問い 2 …v2 の UX 設計における主要課題となる」とするが、PRD の OQ-2 は `[v1]` で「v1 の UX 設計における課題である」としている。PRD 側が正と思われる。addendum 側の更新を推奨。

---

## Summary

| | |
|---|---|
| Structure findings | 18 — High 2 / Medium 9 / Low 4 / Preserve 3 |
| Prose findings | 20 — High 5 / Medium 8 / Low 7 |
| **Total** | **38 — High 7 / Medium 17 / Low 11 / Preserve 3** (P-20 はさらに 5 件の軽微指摘をまとめたもの) |
| Estimated reduction | 約 270 words / 12,444 = **2.2%** |
| Length target | 指定なし — 達成判定なし |

**Comprehension trade-offs:** CUT/CONDENSE はすべて重複の除去か参照への置換であり、失われる情報はない。唯一の注意点は S-05 (§11 の理由文をポインタ化) — §11 を単独で読む用途があるなら現状維持でよい。§3 用語集、§11 一覧、§0 の出自記述はいずれも **PRESERVE** として明示的に温存を推奨した。

**Highest-value first:** P-01 (規範表記の分裂) → P-03 (未定義の中核語) → S-03/S-02 (未決事項の二重管理) → P-02 (壊れた組版) → S-01/S-06 (front-load 違反)。
