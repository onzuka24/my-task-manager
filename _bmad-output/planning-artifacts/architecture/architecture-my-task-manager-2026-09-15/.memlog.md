---
scope: my-task-manager — 常駐型タスク管理ツール v1 (FR-1..FR-8, FR-15, FR-19) および v2 観測層の受け入れ余地
purpose: build-substrate
altitude: feature
updated: 2026-09-15T11:14
---

- (constraint) PRD §7.1/§7.3: 端末内保持のみ。サーバ構成要素・クラウド同期・有償外部サービスへの依存を持たない
- (constraint) PRD §8: 待機時 CPU<1% / メモリ<100MB。ランタイム選択を実質的に絞り込む拘束
- (constraint) PRD FR-1: ホットキー押下から入力受付まで 300ms 以内。常駐プロセスは起動済みであることが前提
- (constraint) PRD FR-3: 現在地と中断メモは切り替え完了時点で確定し、プロセス異常終了をまたいで残る。入力途中の未確定メモは失ってよい
- (constraint) PRD §6.1: v1 スコープは FR-1..FR-8、FR-15、FR-19。本スパインが直接支えるのはこの範囲
- (constraint) PRD §8 可搬性: v2 の FR-11 観測 (アクティブウィンドウ監視) を実装可能な余地を v1 の技術選定で潰さないこと
- (constraint) PRD §10 OQ-1: 対象プラットフォーム未確定。本ランで解消すべき最優先事項
- (direction) 作業モード: Coaching path。load-bearing な判断は選択肢を提示してユーザーが選ぶ
- (direction) 成果物: ARCHITECTURE-SPINE.md に加えて、構造を視覚的に辿れる HTML/SVG のウォークスルーを作成する
- (version) 2026-09 時点の調査: Tauri 安定版は 2.11.5 (2026-07-01 リリース)。Electron 安定版は 43 (2026-06-30, Chromium 150 / Node 24.17.0 / V8 15.0)
- (constraint) 調査結果: Electron 43 の待機時メモリは実測報告で 168-300MB、Tauri 2.x は 30-50MB。PRD §8 の待機時 100MB 未満という上限により Electron 系は選択肢から脱落する。これは好みではなく制約による除外
- (question) v2 の FR-11 観測はプラットフォーム固有 API を要する (macOS: NSWorkspace の frontmost application 監視 + Accessibility 権限)。ランタイム選択は『ネイティブ API に降りられるか』で評価する必要がある
- (decision) OQ-1 解消: v1 のビルド対象は macOS のみ。ただし移植を不可能にしない。ドメインロジックは OS 非依存に保ち、OS 依存は明示的な境界に閉じる
- (decision) ランタイムは Tauri 2.11.5 を採用。理由: (1) Web フロントでユーザーが最も速く書ける=完走確率が最も高い (2) 待機時30-50MBでPRD §8の100MB上限を満たす (3) Rust コアから macOS ネイティブ API に降りられるため v2 観測が実装可能 (4) 移植余地を残せる
- (decision) Electron は不採用。待機時メモリが PRD §8 の上限を 1.7-3 倍超過するため。判断は実測報告に基づく制約充足であり嗜好ではない
- (version) 調査 2026-09: Svelte 5.57 (安定)。SvelteKit 3 は RC、2.x 保守中 — 本件はルーティング/SSR 不要のため SvelteKit は使わず Svelte + Vite とする。tauri-plugin-sql は 2.4.0。tauri-plugin-global-shortcut 2.3.1、tauri-plugin-autostart 2.5.1
- (question) OQ-2 (強い提示と入力先の非奪取の両立) の技術的解: macOS の NSPanel を NSWindowStyleMaskNonactivatingPanel + canBecomeKeyWindow=false で使う。tauri-nspanel が該当。set_hides_on_deactivate(false) と full_screen_auxiliary の collection behavior が必須 — 後者がないとフルスクリーン作業中にパネルが出ない (PRD §7.2 が名指しした状況)
- (decision) 永続化は SQLite。ただし tauri-plugin-sql は不採用 — 同プラグインはフロントから SQL を発行する設計であり『状態の所有者は Rust コア』の不変条件と衝突する。Rust コアが rusqlite/sqlx で直接扱い、フロントにはドメイン操作のみを公開する
- (decision) フロントエンドは Svelte 5.57 + Vite。SvelteKit は不採用 (ルーティング/SSR が不要でオーバーレイ単一画面のため)
- (decision) 提示面は二種類に分ける。(a) オーバーレイ=通常ウィンドウ、フォーカスを取る。中断メモのタイプ入力 (FR-7) に必須 (b) 介入=非活性 NSPanel、canBecomeKeyWindow=false、入力先を奪わない。§7.2 を満たす
- (decision) OQ-2 解消: 介入は非活性パネルで提示し、二択はグローバルホットキー二つで応答する。介入表示中のみ当該ホットキーを登録し、応答後に解除する。手をホームポジションから動かさずに済む
- (decision) 命名規約: 用語集の各語に英語識別子を一つだけ割り当て、spine の対応表で固定する。PRD §3 の『同義語の導入を禁止する』規律をコードにも延長する
- (constraint) 介入パネルには set_hides_on_deactivate(false) と full_screen_auxiliary の collection behavior が必須。後者がないとフルスクリーン作業中に介入が表示されず、PRD §7.2 が名指しした状況で機能しない
- (decision) 配布はローカルビルドの .app を /Applications に置く。署名なし。Gatekeeper は初回の右クリック起動で回避する
- (decision) 自動更新機構を持たない。配信サーバが必要になり PRD §7.1/§7.3 と衝突するため。ビルド→配置を make タスク化し、手順の面倒さが修正を妨げない状態を作る
- (assumption) [ASSUMPTION] 署名なしのため、v2 で FR-11 観測を実装した際に再ビルドごとに Accessibility 権限がリセットされる可能性が高い。v2 着手時に開発用証明書での署名へ移行する必要が生じうる — 再検討条件として記録
- (decision) 設定値 (休息閾値・猶予・腐敗判定期間) は SQLite の設定テーブルに集約する。設定ファイルを別に持つと状態の所有者が二つになり AD-2 が壊れるため
- (decision) ログはローカルファイルのみ。テレメトリ・クラッシュレポートを含む一切の外部送信を行わない (PRD §7.1 の不可侵条件)
- (decision) すべての計時 (連続作業時間・猶予・腐敗判定) は Rust コアが所有する。オーバーレイは閉じている時間のほうが長く、フロントに計時を持たせると FR-15 が成立しないため
- (decision) v1 の時点でコアに ObservationPort を定義し、実装は持たない。v1 を『観測が存在しない』前提で書くと v2 で書き直しになるため
- (event) 全 load-bearing 判断が確定。distill に移行
- (change) レビュー4本 計91件を反映。根本原因は AD-2 の Rule が CurrentPosition しか縛っていなかったこと — v1 の状態6項目に所有者がいなかった。AD-2 を状態所有表に全面書き換え、揮発ビュー状態を3行に限定して列挙
- (change) AD-3 に鮮度規則を追加。Tauri イベントに配送保証はなく、起動時生成+非表示の設計は取りこぼしを最大化するため、表示のたびにコマンドでスナップショットを取り直す規則を明文化
- (change) AD-6 に必須設定3点 (nonactivating_panel / hides_on_deactivate(false) / full_screen_auxiliary) を明記。memlog に必須と記録しながら成果物に移し損ねていた。欠けるとフルスクリーン時にのみ静かに失敗する
- (change) AD-7 を強化: 介入の単一性、Pressed のみ処理 (上流が Pressed+Released の二重発火を not_planned で恒久化)、解除の所有者を単一経路に固定、登録失敗時はクリック応答にフォールバック
- (change) AD-8 に時計源規則を追加。スリープ中は加算せず、スリープ長が休息閾値を超えた場合は休息とみなしてリセット。リセット条件は PRD §3 に従い切り替えでは行わない
- (change) AD-9 を『トレイトの存在』から『コアが消費する入力ストリームの形』へ再定義。{時刻, 作業文脈(省略可), 出所} の三つ組、push型、権限未付与時は無流入
- (decision) AD-14 (資源予算) を新設。PRD §8 の CPU1%/100MB を誰も測る責任を負っていなかった。測定は WKWebView ヘルパープロセスを含む全プロセス合計とする — 除外した値を根拠にできないよう明記
- (decision) AD-15 (抑制は仕様である) を新設。他の全AD は所有と方向のみを規律し『何を作らないか』を縛っていなかったため、全AD 適合のまま進捗バーや常時一覧を足せる状態だった。PRD §5 非目標と §9 逆指標を AD として拘束化
- (change) スタック訂正: global-shortcut 2.3.1→2.3.2、SQLite ドライバを rusqlite 0.40.2 に確定 (sqlx は async ランタイムをコアに持ち込むため不採用)、tauri-nspanel は crates.io 非公開のため git rev 固定を必須化、macos-private-api 必須により App Store 配布は恒久的に不可
- (change) 上流からの差し戻しを PRD に適用: 用語集に『完了』追加、§6.3 の縮退仕様の矛盾を訂正、FR-7 に切り替え履歴の記録を追加
- (event) spine finalized
- (event) 視覚ウォークスルーを walkthrough.html として作成し Artifact 公開: https://claude.ai/code/artifact/9375cf03-82d4-4634-aeb9-c788a7c83e26
